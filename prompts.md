Промты 1-4 я делала в бесплатном cursor, пока он позвалял, бесплатные запросы закончились,поэтому я перехожу на другое бесплатное ии, а именно на qwen studio, промты и результаты я все равно буду прикреплять сюда в md. Промт 5 и далее  на Qwen studio.

ПРОМТЫ:

ПРОМТ1

Ты — ведущий системный аналитик. Проанализируй бриф и уточнения по проекту «Кулинарная студия» (файл прикреплен). Это первый шаг проектирования.  

Фокус анализа: только клиентское мобильное веб-приложение (роль «Клиент»). Источник истины — готовый бэкенд.  

Сгенерируй ответ в виде двух четко разграниченных Markdown-блоков с указанием имени файла для каждого блока. Сами файлы создавать не нужно.  

### БЛОК 1: @docs/[gaps_and_questions.md](https://gaps_and_questions.md/)

1. Выпиши всё, что неясно, противоречиво или отсутствует в брифе с точки зрения логики клиентского приложения.
2. К каждому пробелу сформулируй конкретный, закрытый вопрос заказчику (Артёму).
3. Для каждого вопроса сразу предложи наиболее логичный, простой MVP-вариант решения, чтобы проект можно было реализовать в рамках Летней школы без ожидания ответа.



### БЛОК 2: @docs/[domain_model.md](https://domain_model.md/)

1. Акторы: определи и опиши роли (в рамках контекста Клиента).
2. Сущности: перечисли сущности, с которыми работает приложение. Для каждой строго укажи тип операции: "Только чтение (приходит из бэкенда)" или "Мутация (создается/меняется на клиенте и отправляется в API)".
3. Границы системы: что находится внутри ответственности клиентского приложения, а что скрыто на бэкенде.
4. Глоссарий: составь алфавитный глоссарий ключевых терминов (Слот, Программа, Бронь, Мастер-класс и т.д.) строго по брифу.

СТРОГОЕ ОГРАНИЧЕНИЕ: Ничего не выдумывай лишнего. Пиши максимально подробно. Как только закончишь Блок 2, остановись. В следующем запросе я попрошу тебя сгенерировать требования.

ПРОМТ2

Отлично, анализ и доменная модель приняты. Теперь переходим ко второму шагу. На основе предыдущего контекста сгенерируй Блок 3 с требованиями.   

Выдай ответ в виде единого Markdown-блока с указанием имени файла.  

### БЛОК 3: @docs/[requirements.md](http://requirements.md)

Выведи требования. Каждый элемент обязан иметь: Уникальный ID (например, BR-001, FR-001), Формулировку, Приоритет и Ссылку на источник (пункт или раздел брифа).  

Разделяй требования по уровням:  

1. Бизнес-требования (BR): верхнеуровневые цели клиента.
2. Функциональные требования (FR/ФТ): детальное описание того, что должно делать именно приложение.
3. Нефункциональные требования (NFR): минимум 5 релевантных требований (адаптивная верстка под mobile web, время отклика интерфейса, безопасная передача личных данных (телефон, аллергии) по HTTPS, офлайн-устойчивость/уведомление о потере сети, интуитивно понятный интерфейс). Не смешивай с FR.
4. User Stories: с приоритетом (Must, Should, Could) по формату: "Как <роль>, я хочу <действие>, чтобы <ценность>".
5. Use Cases: пошаговые основные сценарии (счастливый путь) для ключевых User Stories.

Пиши максимально подробно, не сокращай сценарии. Если не будет хватать места, остановись, я попрошу продолжить.

ПРОМТ 3

Ты — ведущий системный архитектор. На основе доменной модели (@[domain_model.md](https://domain_model.md/)) и требований (@[requirements.md](http://requirements.md)), созданных ранее для проекта «Кулинарная студия», сформируй технический фундамент для реализации MVP клиентского мобильного веб-приложения на чистом Vanilla JS.  

Выдай ответ в виде двух Markdown-блоков (для файлов). Сами файлы не создавай.  

### БЛОК 4: [architecture.md](http://architecture.md)

1. **Архитектурный паттерн**: опиши структуру модульного SPA без фреймворков:

- Разделение на слои: UI (компоненты, рендер-функции), бизнес-логика (сервисы/use cases), API-клиент (асинхронные HTTP-запросы).  
- Организация хэш-роутинга (простой роутер, смена экранов без перезагрузки страницы).

1. **Схема данных (Client-side Data Model)**: перечисли сущности, их ключевые атрибуты с типами, источник ("только чтение" или "мутация"). Приведи примеры JSON-представлений для Слота и Брони.
2. **Диаграмма взаимодействия**: текстовое описание сценария "Создание бронирования" (createBooking) с ветками:

- Успешный ответ (201 Created).  
- Конфликт (409 Conflict, слот занят).  
- Слот отменён/недоступен (410 Gone).



### БЛОК 5: @api/openapi.yaml

Сгенерируй спецификацию OpenAPI 3.0 в формате YAML для эндпоинта POST /bookings.  
Идентификация клиента осуществляется по номеру телефона (поле phone в теле запроса). Никаких токенов или заголовков авторизации не нужно.  
В спецификации опиши:  

- Тело запроса (body): slotId (строка), phone (строка), equipmentOptions (объект), allergies (строка).  
- Ответы:  
- 201 Created: { bookingId: string, status: "confirmed" }  
- 409 Conflict: { error: "Slot already booked" }  
- 410 Gone: { error: "Slot is no longer available" }  
- Никаких обязательных заголовков, кроме Content-Type.

Соблюдай MVP-структуру, без избыточных полей.

ПРОМТ 4

Создай два файла в проекте с тем содержимым, что сгенерировал. Файл @[architecture.md](http://architecture.md) - запиши в него всё содержимое по блоку 4. Файл api/openapi.yaml - запиши в него все содержимое по блоку 5. Убедись, что каждый файл содержит полный код/текст без сокращений. Если нужно то создай папку api в проекте,если ее еще нет.

!

У меня закончились запросы в бесплатном cursor. Поэтому я перехожу на другой бесплатный ии, но промты и результаты я буду прикреплять сюда!

!

ПРОМТ 5(Файлы md прошлые(архитектура и т.д) были прикреплены в запрос. Полученный код я отправила в mvp_web_appcode.md

Ты — фронтенд‑разработчик. У нас есть проект с архитектурой в @[architecture.md](http://architecture.md), требованиями в @[requirements.md](http://requirements.md) и API‑спецификацией в @api/openapi.yaml.  

Создай только новые файлы для MVP (Vanilla JS, мобильное веб‑приложение). Существующие файлы не изменяй.  

Нужно создать следующие файлы с полным кодом:  

- index.html (в корне проекта)  
- src/app.js  
- src/api.js  
- src/mockData.js  
- src/styles.css

Реализуй три фичи:  

1. Расписание мастер‑классов: список карточек с занятиями из моков (дата в формате «ДД.ММ.ГГГГ», время — «ЧЧ:ММ», название мастер‑класса, место проведения). Если занятий нет — покажи сообщение «Нет доступных мастер‑классов». Во время загрузки — покажи индикатор загрузки.
2. Выбор мастер‑класса и запись: по клику на карточку открывается модальное окно с формой. Поля: имя (обязательно, минимум 2 символа), телефон (обязательно, простая валидация: допускаются цифры, +7 или 8, без спецсимволов кроме плюса), аллергии / диетические ограничения (опционально, текстовое поле), прокат оборудования — два чекбокса: «Нужен фартук» и «Нужны ножи» (оба опциональны). Модальное окно закрывается по кнопке «Закрыть», по крестику и по клику в затемнённую область вне окна. При открытии модального окна ставь фокус на поле «Имя». Не открывай модальное окно повторно, если оно уже открыто.
3. Отправка записи: используй моки вместо реального API. В моке `createBooking`:

- Если `name="Тест 409"` — отклони промис с ошибкой `{ status: 409, message: "Вы уже записаны на это занятие" }`.  
- Если `name="Тест 410"` — отклони с `{ status: 410, message: "Место на мастер‑классе закончилось" }`.  
- Параметр `simulateNetworkError` передавай как отдельный аргумент функции `createBooking(formData, simulateNetworkError)`. По умолчанию он должен быть `false`. Если `simulateNetworkError === true` — сразу отклони промис без статуса (эмуляция сетевой ошибки).  
- Иначе — разреши промис объектом `{ status: 201, data: { bookingId: "BKK-XXXX" } }`, где XXXX — случайная строка из 4 цифр.

Обработай состояния: загрузка, успех, 409, 410, сетевая ошибка. Покажи пользователю понятное сообщение для каждого случая. Во время отправки кнопка «Записаться» должна быть неактивной, текст на ней — «Отправка…». При успехе (201) покажи подтверждение «Вы успешно записаны на мастер‑класс! Ваш номер брони: …», закрой модальное окно и оставь карточку занятия без изменений. При повторном открытии модального окна все предыдущие сообщения об ошибках/успехах должны очищаться.  

Требования:  

- Чистый JavaScript (ES6 модули), HTML5, CSS3, без фреймворков.  
- Адаптивная вёрстка под мобильные устройства: карточки в одну колонку, читаемые шрифты, кликабельные области не менее 44px.  
- Архитектура: UI‑компоненты (отдельные функции для рендера расписания, модального окна, сообщений), сервисы (логика форм, валидация), API‑клиент (функции запросов, сейчас на моках).  
- Занятия в моках должны иметь поля: `id`, `date`, `time`, `title`, `location`. Используй их для рендера карточек.  
- Проект запускать через локальный HTTP‑сервер (например, `python -m http.server`). Прямое открытие `index.html` работать не будет из‑за CORS для модулей.  
- В `index.html` подключай главный скрипт как `<script type="module" src="src/app.js"></script>`. Все остальные JS‑файлы импортируй через `import`.

Сгенерируй в конце ответа список из 6–8 тест‑кейсов (в формате: ID, заголовок, шаги, ожидаемый результат) для проверки реализованных фич и возможных багов. Каждый тест‑кейс должен соответствовать одному из сценариев: успех, 409, 410, сетевая ошибка, валидация полей, закрытие модального окна.  

ВАЖНО: Если весь код не помещается в один ответ, создай файлы в строгом порядке:  

1. Сначала index.html, src/styles.css, src/mockData.js.
2. Потом src/app.js и src/api.js.

Не используй сокращений вида «// …». Выводи полный код для каждого файла, чтобы можно было скопировать и сразу положить в проект.

ПРОМТ 6

У меня есть веб-приложение, которое не запускается из-за ошибок (текст ошибок ниже).  
Пожалуйста, исправь код так, чтобы он заработал в одном HTML-файле без внешних CSS и JS файлов.  

Требования:  

- Объедини всё в один HTML-файл.
- Все стили помести внутрь тега  style в head.  
- Весь JavaScript помести внутрь одного тега .
- Исправь ошибку с экспортами (все export должны быть только на верхнем уровне модуля, не внутри функций или условий).  
- Убедись, что HTML-разметка содержит контейнеры для каталога и модального окна (если они используются).  
- Сделай так, чтобы файл открывался просто двойным щелчком в браузере без сервера (не должно быть внешних импортов, всё внутри одного файла).

Вот ошибки из консоли:  
Ресурс с «[http://127.0.0.1:5500/src/styles.css»](http://127.0.0.1:5500/src/styles.css») был заблокирован из-за несоответствия MIME-типа («text/html») (X-Content-Type-Options: nosniff). testweb.html  
Загрузка модуля по адресу «[http://127.0.0.1:5500/src/app.js»](http://127.0.0.1:5500/src/app.js») была заблокирована из-за неразрешённого MIME-типа («text/html»). testweb.html  
Live reload enabled. testweb.html:495:13  
Uncaught SyntaxError: export declarations may only appear at top level of a module  

Вот мой текущий код:(вставила для ии сюда неработующий код, чтобы исправил, сюда думаю вставлять его излишне(слишком большой))

После этого промта все заработало(скриншоты приложу в отчете)

ПРОМТ 7

Ты — опытный QA-инженер. Проведи глубокий анализ предоставленного ранее кода одностраничного веб-приложения для записи на кулинарные мастер-классы.  
  
Найди 3-4 **реальных, серьёзных бага**, которые могут сломать логику или UX. Не предлагай очевидные ошибки вроде «нет фокуса». Ищи проблемы, которые реально могут возникнуть у пользователя:  
  
- Неправильная обработка граничных случаев (пустой ответ от сервера, отсутствие слотов, очень длинное имя, спецсимволы в телефоне).  
- Ошибки в бизнес-логике (например, можно записаться на слот повторно, не блокируется кнопка после успеха, не очищается форма после закрытия, сообщения об ошибках накапливаются).  
- Проблемы с доступностью (клавиатура, скринридеры) или адаптивностью (на очень маленьком экране форма вылезает за границы).  
- Ошибки в работе с модальным окном (можно прокручивать страницу за модалкой, закрытие по Escape не работает, данные не сбрасываются).  
- Ошибки валидации (телефон с буквами пропускается, имя из пробелов считается валидным).  
  
Для каждого бага:  
1. Опиши **симптом** (что видит пользователь).  
2. Укажи **причину** в коде (какой участок отвечает).  
3. Предложи **конкретное исправление** (покажи изменённый код или чётко опиши, что заменить).  
  
Также на основе этого анализа сгенерируй 6-8 тест-кейсов, которые **обязательно включают** сценарии для найденных багов (позитивные и негативные). Формат тест-кейса: ID, заголовок, предусловия, шаги, ожидаемый результат.  
  
код(вставляю весь для ии) тут излишне.

!

Также хочу сказать, что эти промты проходили множественные модификации(перед тем как получились эти самые 7) через разные бесплатные ии(deepseek,gemeni,алиса)!